apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-service
  namespace: insurance-app
  labels:
    app: customer-service
    tags.datadoghq.com/env: demo2
    tags.datadoghq.com/service: customer-service
    tags.datadoghq.com/version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: customer-service
  template:
    metadata:
      labels:
        app: customer-service
        tags.datadoghq.com/env: demo2
        tags.datadoghq.com/service: customer-service
        tags.datadoghq.com/version: "1.0.0"
        admission.datadoghq.com/enabled: "true"
      annotations:
        admission.datadoghq.com/enabled: "true"
    spec:
      containers:
      - name: customer-service
        image: wwongpai/insurance-customer-service:1.0.3
        ports:
        - containerPort: 8083
        env:
        - name: POSTGRES_HOST
          value: postgres-service
        - name: POSTGRES_PORT
          value: "5432"
        - name: POSTGRES_DB
          value: insurancedb
        - name: POSTGRES_USER
          value: insuranceuser
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: password
        - name: POLICY_SERVICE_URL
          value: http://policy-service:8081
        - name: PORT
          value: "8083"
        - name: DD_PROFILING_ENABLED
          value: "true"
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8083
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /health
            port: 8083
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: customer-service
  namespace: insurance-app
spec:
  selector:
    app: customer-service
  ports:
  - port: 8083
    targetPort: 8083
  type: ClusterIP
